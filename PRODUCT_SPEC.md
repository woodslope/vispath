# VisPath · 视觉路径 MVP 产品说明

> 状态：已确认的实现基线 v0.1
> 日期：2026-07-12

## 1. 产品定位

VisPath（视觉路径）帮助设计师和内容创作者把一个原始想法，拆成围绕明确视觉维度的多套提示词，并直接生成或复制到外部生图工具进行比较。

它解决的是“我想比较几种视觉路径，但不想手动重写多套提示词”的问题，不承诺用少量图片证明严格的因果结论。

## 2. MVP 用户流程

### 2.1 输入

用户可以：

- 粘贴原始提示词。
- 上传一张参考图。
- 同时提供提示词和参考图。

参考图的作用由用户选择：灵感参考、内容基准或继续编辑的底图。

### 2.2 选择任务类型

当前内置六种类型：

- 海报
- 活动主视觉 / 线下物料
- 品牌应用 / VI 展示
- Landing Page
- Dashboard 方向板
- 卡通 / IP 形象

类型只影响可用维度、默认字段和提示词编译规则，不改变主流程。

### 2.3 选择探索维度

首版默认一次选择一个维度，支持：

- 设计风格
- 卡通 / 角色风格
- 色彩风格
- 版式风格
- 构图与镜头
- 材质与质感
- 信息密度
- 品牌气质

多维度组合属于高级模式，首版先保留数据结构，不作为默认入口。

### 2.4 选择输出数量

用户通常选择生成 2 至 6 套提示词，默认 3 套。需要由模型根据任务与用途动态命名方向的“设计风格”“卡通 / 角色风格”和品牌应用 / VI 展示的动态“设计风格”额外支持 8 套和 10 套；固定候选维度仍最多生成 6 套。

品牌应用生成 10 套方向时，系统维护可继续扩展的“已验证优先风格锚点”。对于餐饮摊车、市集摊位、餐饮快闪、节庆售卖等匹配场景，当前稳定保留“国潮喜庆风”“高饱和街头快闪风”“萌趣插画风”，其余方向继续根据载体动态生成；明显不匹配的载体不强制套用。三个方向必须保持各自语义，不能分别被普通东方传统装饰、街头涂鸦或泛化手绘插画替代。

“提示词方案数量”和“每套生成的图片数量”是两个独立设置。首版默认每套生成 1 张图片。

### 2.5 生成变量方案

用户点击“生成视觉方向”后，文本 / 多模态 API 在一次调用中完成：

1. 解析输入内容。
2. 建立 PromptBlueprint。
3. 锁定固定内容。
4. 围绕选定维度生成指定数量的变量方案。
5. 为每个方案生成完整提示词和变化说明。

用户不需要先阅读一份独立分析报告。

模型返回的有效方案少于请求数量时，方案区保留并立即展示已有结果，不因数量不足清空整批。界面显示“已返回 X / Y 套”和缺少数量；用户可以直接使用已有方案，也可以发起补齐。补齐请求只生成缺少数量，继承同一份固定蓝图，并排除已有方向、近义方向和同一主风格家族。若补齐仍不足或失败，已经展示的方案继续保留。

### 2.6 方案卡

每套方案显示：

- 方案名称。
- 本次变化的维度和值。
- 保持不变的内容。
- 完整提示词。
- 可编辑提示词。
- 复制提示词。
- 选择当前方案。

用户可以选择一套、多套或全部提示词，通过统一的“提交生成”操作进入图片生成画板。提示词卡片自身不直接调用生图 API。

### 2.7 生成画板与继续细化

最右侧是持续累积的生成任务和历史结果瀑布流。每次提交都会形成新的批次和不可变提示词快照。

用户可以：

- 查看生成中、成功和失败状态。
- 保留本轮和历史批次结果。
- 只重试某一张图片。
- 复制某套提示词到外部工具。
- 选中一个方向作为下一轮基础。
- 继续选择另一个维度生成下一轮方案。

## 3. API 边界

### 文本 / 多模态 API

负责解析文字或参考图，返回结构化 PromptBlueprint 和 PromptVariant 列表。

### 生图 API

只接收用户确认后的最终提示词和生成参数，负责生成图片，不负责判断变量或修改用户意图。

首版只接入一个实际验证过的生图服务。多供应商能力以后通过 ProviderAdapter 扩展，不在 MVP 中同时兼容所有 OpenAI-compatible 服务。

## 4. 产品规则

- 默认一次只探索一个维度，确保方案差异清楚。
- 方案卡必须显示“变化项”和“固定项”。
- 原始输入始终保留，AI 不得静默覆盖。
- AI 生成的结构和提示词必须允许用户编辑。
- 参考图是否作为灵感、内容基准或编辑底图必须明确显示。
- 不把一次画板比较称为科学实验，不输出未经用户确认的结论。
- 不能确认的模型能力（如种子、参考图、比例）必须显示为未确认，不得假设支持。

## 5. 暂不做

- 账号、云同步和多人协作。
- 复杂历史、知识库和资产管理。
- 小红书发布文案。
- 自动视觉评分和“最佳方案”判断。
- 多维度完整笛卡尔积生成。
- 多供应商统一参数的伪兼容。

## 6. MVP 验收标准

- 用户能用提示词或图片启动一次探索。
- 类型选择会过滤可用探索维度。
- 选择一个维度和支持的方案数量后，能得到对应数量的方案卡；模型少返时先显示有效部分，并可继续补齐。
- 每张方案卡都能看出变化项、固定项和完整提示词。
- 提示词可以复制，不配置生图 API 也能完成主流程。
- 配置生图 API 后，能按方案生成图片并单独重试失败项。
- 选中一张画板后，能基于它继续下一轮探索。
- 页面刷新不会丢失当前输入和已生成结果。
